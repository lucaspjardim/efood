import Restaurants from "../../models/Restaurants";
// import Product from '../Product'
// import { Container, List } from './styes'

export type Props = {
  title: string
  restaurant: Restaurants[]
}

const ProductsList =({title, restaurant}: Props) => (
  
)
